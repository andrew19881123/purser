[defaults]
# Roles are resolved relative to this directory, so playbooks can reference
# the purser_agent role without a full path.
roles_path = roles

# Default inventory directory (create inventory/ with your host definitions)
inventory = inventory/

# Reduce noise in output
display_skipped_hosts = false
