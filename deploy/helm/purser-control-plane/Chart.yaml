apiVersion: v2
name: purser-control-plane
description: >
  Purser Control Plane: registry, scheduler, PKI, auth API.
  Deploy this chart once; configure multiple Data Planes to connect to it.
type: application
# Chart version — bump on any change to the chart itself.
version: 0.5.0
# Version of the Purser application shipped by this chart (image tag default).
appVersion: "0.5.0"
keywords:
  - ai
  - llm
  - inference
  - orchestration
  - control-plane
home: https://github.com/andrew19881123/purser
sources:
  - https://github.com/andrew19881123/purser
