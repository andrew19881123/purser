apiVersion: v2
name: purser
description: >-
  Purser enterprise control plane — Control Plane (registry + PKI +
  RegistrationService), API Gateway (OpenAI-compatible) and Operator UI. Agents
  run OUTSIDE the cluster as host packages and join the Control Plane over
  gRPC/mTLS; expose the Control Plane via LoadBalancer/NodePort to reach them.
type: application
# Chart version — bump on any change to the chart itself.
version: 0.1.0
# Version of the Purser application shipped by this chart (image tag default).
appVersion: "0.1.0"
keywords:
  - purser
  - llm
  - inference
  - control-plane
  - gateway
home: https://github.com/purser/purser
sources:
  - https://github.com/purser/purser
